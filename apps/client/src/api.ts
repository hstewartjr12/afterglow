import type {LibraryEntry,LibraryInput,Preferences,Recommendation,VnDetail,VndbTag,VnSummary} from "@afterglow/shared";
export type VnSearchFilters={q:string;platform:string;length:string;year:string;rating:string;sort:string};
async function request<T>(url:string,options?:RequestInit):Promise<T>{const r=await fetch(url,{...options,headers:{"Content-Type":"application/json",...options?.headers}});if(!r.ok){const body=await r.json().catch(()=>({error:"Request failed"}));throw new Error(body.error)}return r.status===204?undefined as T:r.json()}
export const api={search:(filters:VnSearchFilters)=>{const params=new URLSearchParams({q:filters.q,page:"1",sort:filters.sort});for(const key of ["platform","length","year","rating"] as const)if(filters[key])params.set(key,filters[key]);return request<{results:VnDetail[];count?:number;more?:boolean}>(`/api/vndb/search?${params}`)},tags:(q:string,page=1,category="")=>request<{results:VndbTag[];recordCount:number;usableOnPage:number;page:number;more:boolean}>(`/api/vndb/tags?q=${encodeURIComponent(q)}&page=${page}${category?`&category=${category}`:""}`),detail:(id:string)=>request<VnDetail>(`/api/vndb/vn/${id}`),library:()=>request<LibraryEntry[]>("/api/library"),saveLibrary:(id:string,input:LibraryInput)=>request<LibraryEntry>(`/api/library/${id}`,{method:"PUT",body:JSON.stringify(input)}),removeLibrary:(id:string)=>request<void>(`/api/library/${id}`,{method:"DELETE"}),preferences:()=>request<Preferences>("/api/preferences"),savePreferences:(p:Preferences)=>request<Preferences>("/api/preferences",{method:"PUT",body:JSON.stringify(p)}),recommendations:()=>request<Recommendation[]>("/api/recommendations"),scoreCandidates:(vns:VnSummary[])=>request<Recommendation[]>("/api/recommendations/score",{method:"POST",body:JSON.stringify({vns})}),recommendation:(id:string)=>request<Recommendation>(`/api/recommendations/${id}`)};
